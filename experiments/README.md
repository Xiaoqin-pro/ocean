# Experiment registry

Every formal experiment must bind its result to a Git commit, branch, split version, configuration, random seed, checkpoint hash, and environment record. `v1` is retained as an invalid-for-paper engineering record; do not overwrite or delete it.
