experiment:
  name: utd_suim_test
  branch: experiment/sgre-seg
  seed: 20260811
  output_dir: outputs/utd_seg_suim
data:
  test_csv: data/suim_processed/splits/v2_scene_grouped_deduplicated/test.csv
  image_size: 384
  degradation_registry: configs/degradation_pilot.yaml
  test_images: 110
model:
  pretrained_model: nvidia/mit-b0
  source_checkpoint: outputs/ft_reliability_suim_pilot_v1_2/segformer/formal/B/checkpoints/final.pt
  hidden_sizes: [32, 64, 160, 256]
  num_classes: 8
  rectifier_reduction: 4
  checkpoint: outputs/utd_seg_suim/formal/UTD/checkpoints/final.pt
  comparator_checkpoint: outputs/dts_seg_gate0/formal/F4/checkpoints/final.pt
evaluation:
  conditions: [clean, color_s1, color_s2, color_s3, turbidity_s1, turbidity_s2, turbidity_s3, lowlight_s1, lowlight_s2, lowlight_s3, blur_s1, blur_s2, blur_s3]
  official_suim_test: true
